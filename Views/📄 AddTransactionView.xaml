<Window x:Class="ByteinoFinance.Views.___AddTransactionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ByteinoFinance.Views"
        xmlns:viewModels="clr-namespace:ByteinoFinance.ViewModels"
        mc:Ignorable="d"
        Title="Add Transaction" Height="400" Width="300"
        WindowStartupLocation="CenterOwner">
    <Window.DataContext>
        <viewModels:AddTransactionViewModel />
    </Window.DataContext>
    <Grid Margin="20">
        <StackPanel >
            <TextBox Text="{Binding Description}" />
            <TextBox Text="{Binding Category}" />
            <ComboBox SelectedItem="{Binding Type}">
                <ComboBoxItem>Income</ComboBoxItem>
                <ComboBoxItem>Expense</ComboBoxItem>
            </ComboBox>
            <TextBox  Text="{Binding Amount}" />
            <DatePicker SelectedDate="{Binding Date}" />
            <Button Content="Add" Command="{Binding AddCommand}" />
        </StackPanel>
    </Grid>
</Window>
