<Window x:Class="ByteinoFinance.Views.AddTransactionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:vm="clr-namespace:ByteinoFinance.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ByteinoFinance.Views"
             mc:Ignorable="d"  WindowStartupLocation="CenterOwner"
             d:DesignHeight="400" d:DesignWidth="400" Width="400" Height="400">

    <Grid Margin="20" Background="#FFA7A7A7">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">

            <TextBlock Text="Amount:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Amount}" Margin="0,0,0,10" />

            <TextBlock Text="Transaction Type:" Margin="0,0,0,5"/>
            <ComboBox ItemsSource="{Binding Path=TransactionTypes}" SelectedItem="{Binding Type}" Margin="0,0,0,10"></ComboBox>
            <TextBlock Text="Category:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Category}" Margin="0,0,0,10" />

            <TextBlock Text="Description:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Description}" Margin="0,0,0,10" />

            <TextBlock Text="Date:" Margin="0,0,0,5"/>
            <DatePicker SelectedDate="{Binding Date}" Margin="0,0,0,10" />

            <Button Content="Save Transaction"  Command="{Binding SaveTransactionCommand}" />
        </StackPanel>
    </Grid>
</Window>
