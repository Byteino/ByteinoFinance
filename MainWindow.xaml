<Window x:Class="ByteinoFinance.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ByteinoFinance.Views"
        mc:Ignorable="d"
        Title="ByteinoFinance" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FA">

    <Window.Resources>
        <Style x:Key="TotalIncome" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Green"/>
        </Style>
        <Style  x:Key="TotalExpense" TargetType="TextBlock">
            <Setter Property="Foreground" Value="DarkRed"/>
        </Style>

    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>



        <!-- Header: totals -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="0" Margin="0,4">
            <TextBlock Text="Total Income:" FontWeight="SemiBold" Style="{StaticResource TotalIncome}" Margin="0,0,6,0"/>
            <TextBlock Text="{Binding TotalIncome, StringFormat={}{0:C}}" Style="{StaticResource TotalIncome}" Margin="0,0,20,0"/>
            <TextBlock Text="Total Expense:" FontWeight="SemiBold" Style="{StaticResource TotalExpense}" Margin="0,0,6,0"/>
            <TextBlock Text="{Binding TotalExpense, StringFormat={}{0:C}}" Style="{StaticResource TotalExpense}" Margin="0,0,20,0"/>
        </StackPanel>

        <!-- Add button -->
        <Button Content="Add Transaction" Width="140" Height="36" Grid.Row="1"
                HorizontalAlignment="Left" Margin="0,8,0,8"
                Click="OpenAddTransaction_Click"/>

        <!-- Transaction list placeholder (UserControl inserted from code-behind) -->
        <ContentControl x:Name="TransactionListControl" Grid.Row="2" />
    </Grid>
</Window>
